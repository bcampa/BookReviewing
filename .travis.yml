language: csharp
solution: BookReviewing.sln
mono: none
dotnet: 3.1.0
sudo: required
install:
    - sudo apt-get install nuget
    - nuget restore BookReviewing.sln
script:
    - dotnet build
    - dotnet test
